{
  "name": "@erp/shared",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/esm/index.d.ts",
      "import": "./dist/esm/index.js",
      "require": "./dist/cjs/index.js"
    }
  },
  "types": "./dist/esm/index.d.ts",
  "scripts": {
    "build": "node -e \"const fs = require('node:fs'); fs.rmSync('dist', { recursive: true, force: true });\" && tsc -p tsconfig.build.json && tsc -p tsconfig.build.cjs.json && node -e \"const fs = require('node:fs'); fs.mkdirSync('dist/cjs', { recursive: true }); fs.writeFileSync('dist/cjs/package.json', JSON.stringify({ type: 'commonjs' }, null, 2) + '\\n');\" && node -e \"import('./dist/esm/index.js').then(() => console.log('ok')).catch((error) => { console.error(error.code || error.name); console.error(error.message); process.exit(1); })\" && node -e \"try { const mod = require('./dist/cjs/index.js'); console.log(Object.keys(mod).includes('salesApprovalStatuses') ? 'ok' : 'missing'); } catch (error) { console.error(error.code || error.name); console.error(error.message); process.exit(1); }\"",
    "test": "vitest run",
    "lint": "echo 'Scaffolding pending: packages/shared lint is not wired yet.'",
    "format": "echo 'Scaffolding pending: packages/shared format is not wired yet.'"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
