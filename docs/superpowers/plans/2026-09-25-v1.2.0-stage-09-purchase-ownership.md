# v1.2.0 第 09 段：采购归属与来源询价

- [x] R16-01：记录提交比价的采购员；来源询价转销售后，采购单只归该采购员。

> runtime 与 Prisma 都将实际比价提交人写入询价快照；销售审批按此归属采购单，采购单保存和提交时校验指定负责人。

- [x] R16-02～03：无确定采购员的销售单在审批后进入待分配；采购主管或老板指定后才生成采购单。页面、接口、待办一致。

> 增加待分配状态、采购负责人分配页和授权接口；采购主管/老板可分配，销售主管无权分配。建单成功后才推进销售单状态；失败可由原负责人重试，多产品拆单重试补齐缺失单据。

- [x] R17-01～02：采购主管审批采购单时，在采购单信息下看到关联询价；无关联询价不显示。

> 仅审批节点的授权角色请求来源询价摘要；展示客户、报价、比价提交人、供应商报价和确认采购价。历史无关联采购单不显示。

- [x] 测试两种存储、越权和重复请求；运行 `make ci`，检查关键页面，完成后停下待验收。

> `make ci` 通过：shared 42、web 382、api 552 项测试；内置浏览器已打开采购列表、负责人分配页和历史采购详情。现有本地数据无本段新建的来源询价采购单，因此浏览器未实操新建后的完整审批链；正向/反向页面渲染及 API 数据由测试覆盖。Prisma 使用现有 JSON payload，无 schema 迁移；未连接真实 MySQL 执行集成验证。

约束：不回填历史询价提交人；无记录的历史销售单按待分配处理。无需修改 Prisma 表结构，新增元数据保存在现有 `BusinessDocument.payload` JSON 中。
