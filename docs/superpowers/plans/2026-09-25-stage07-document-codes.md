# 第 07 段：单据编号与客户订单号

范围：R12-01～R12-03、R15-01。仅新建单据使用新编码，不回填旧单。

- [x] 给编码规则与自动编号增加先失败的业务测试；核对运行时与 Prisma 两种路径。
  > 先运行 shared 编码规则测试得到预期失败；补充销售、采购拆单与 Prisma 入出库编号测试。Prisma 通过服务测试替身验证，未连接真实 MySQL。
- [x] 新编号使用两位年份且无连字符；移除客户订单号单独规则入口，销售单默认同号且销售可在草稿提交时修改。
  > 新单使用紧凑年月日加流水号；保留旧单原编号与已有运行时规则数据，不做回填或数据库迁移。
- [x] 从来源销售单生成关联采购单号，拆多单按 `-1`、`-2` 编号，前后端保持来源信息一致。
  > 转单时从已保存的来源销售单读取销售单号及客户订单号；浏览器验证单张采购单 `S2609250100`→`C2609250100`，自动化测试验证拆单后缀。
- [x] 将销售单客户订单号移至靠后的非关键区域，验证保存与状态流转。
  > 浏览器验证销售将默认客户订单号改为 `CUSTOMER-STAGE07-01` 后提交，审批生成采购单，来源字段一致。
- [x] 运行相关测试与 `make ci`；有条件时用页面验证新建和转单，并记录限制。
  > `make ci` 通过：shared 42、web 374、API 538 个测试。使用隔离 runtime 数据目录与端口 3300/3301 验证规则页、销售新建及提交、审批与采购转单；Prisma 仅通过自动化替身验证，未连接真实 MySQL。未执行部署或旧单回填。
