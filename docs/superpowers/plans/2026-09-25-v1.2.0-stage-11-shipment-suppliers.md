# v1.2.0 第 11 段：供应商合作分类与发货批次布局

范围：R01-01～R01-05、R18-01～R18-02。第 10 段已提交，本段只处理新发货触发的供应商状态和发货批次新建、详情页；列表及历史单据保持原行为。

- [x] 为往来单位增加合作分类和增量 Prisma 迁移；runtime 历史记录缺字段时按未合作读取，新建供应商默认未合作。
  > `Counterparty.cooperationStatus` 新增默认值；迁移只加列，不扫描既有发货记录。
- [x] 老板在往来单位页面手动修改分类，接口拦截其他角色改分类；补权限与保存测试。
  > 老板和管理员可编辑，普通采购不能通过页面或接口修改分类；接口权限测试通过。
- [x] 新发货取关联采购单供应商：按关联 ID 或手填名称精确匹配，匹配则改已合作，否则建已合作档案；部分发货同样生效，不扫描旧批次。覆盖 runtime、Prisma 和重复请求业务测试。
  > runtime 的部分发货联动、重发货、手填同名复用及 Prisma 的按 ID/名称匹配、无匹配建档均有测试；全局幂等拦截器沿用第 03 段。自动建档直接写已合作，避免中间状态。
- [x] 将发货批次新建、详情改成紧凑表格式展示，保留原字段、提交与后续动作；补页面测试。
  > 新建表单分格；详情批次概览与台账变为表格，发货明细和操作保持；页面回归通过。
- [x] 运行相关测试和 `make ci`，尽可能在页面核对；记录结果并按仓库提交约定提交。
  > `make ci` 通过：shared 16 套/42 项，web 74 套/388 项，api 109 套/570 项；浏览器只读核对新建、详情及供应商列表/编辑。未在真实 MySQL 执行迁移，也未在用户现有业务数据上新建发货以免产生验收数据。
