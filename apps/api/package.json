{
  "name": "api",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "prestart:dev": "prisma generate",
    "start:dev": "tsx watch src/main.ts",
    "prebuild": "prisma generate",
    "build": "tsc -p tsconfig.build.json",
    "pretest": "prisma generate",
    "test": "jest --runInBand",
    "prisma:validate": "prisma validate",
    "prisma:generate": "prisma generate",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "prisma:seed": "node -r ts-node/register prisma/seed.ts",
    "db:verify": "node -r ts-node/register src/storage/verify-prisma-db.ts",
    "lint": "echo 'Scaffolding pending: apps/api lint is not wired yet.'",
    "format": "echo 'Scaffolding pending: apps/api format is not wired yet.'"
  },
  "prisma": {
    "seed": "node -r ts-node/register prisma/seed.ts"
  },
  "dependencies": {
    "@erp/shared": "workspace:*",
    "@nestjs/common": "^10.4.12",
    "@nestjs/core": "^10.4.12",
    "@nestjs/platform-express": "^10.4.12",
    "@prisma/adapter-mariadb": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "express": "^4.22.1",
    "mariadb": "^3.5.3",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@nestjs/testing": "^10.4.12",
    "@types/jest": "^29.5.12",
    "@types/node": "^22.10.2",
    "@types/supertest": "^6.0.2",
    "jest": "^29.7.0",
    "prisma": "^7.8.0",
    "supertest": "^7.0.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
